# NUM · Counts

Reusable quantity/count ranges that drive conditional logic.

## Category: Kids & Pets (catcode **KP**)
- Range **1–15**.
- **Tile A** layout = 1–10 distinct · **Tile B** = 11–15 distinct.
- Chosen kids/pets **cycle** to fill every slot (1,2,3,1,2,3…) via linked layers — the
  pattern always looks full regardless of count.

Teeinblue setup: Number option 1–15 → conditional logic reveals that many
Character + Photo input pairs, shows the matching Tile group, and cycles the faces/bodies.

## Future categories (same type)
- Apparel Sizes (XS–5XL) — drives per-size print panel + price tier
